import { SimulationChildForm } from './simulation-child-form';

describe('SimulationChild', () => {
  it('should create an instance', () => {
    expect(new SimulationChildForm()).toBeTruthy();
  });
});
